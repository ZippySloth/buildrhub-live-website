[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Force HTTPS
[[redirects]]
  from = "http://buildrhub.io/*"
  to = "https://buildrhub.io/:splat"
  status = 301
  force = true

# www → root redirect
[[redirects]]
  from = "https://www.buildrhub.io/*"
  to = "https://buildrhub.io/:splat"
  status = 301
  force = true

# SPA routing — must be last (fallback)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Security + SEO headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"

# Cache static assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "*.png"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
